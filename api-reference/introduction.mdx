---
title: 'API Reference Documentation'
description: 'Complete REST API documentation for the TaskFlow platform'
icon: 'code'
---

## Overview

Welcome to the TaskFlow API documentation. This REST API allows you to programmatically manage tasks, users, and projects within the TaskFlow platform. All endpoints follow REST principles and return JSON responses.

**Base URL:** `https://api.taskflow.com/v1`

**Current Version:** v1.2.0

## Authentication

All API requests require authentication using a Bearer token. Include your API key in the Authorization header:

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://api.taskflow.com/v1/tasks
```

### Obtaining an API Key

1. Log in to your TaskFlow dashboard
2. Navigate to **Settings** > **API Keys**
3. Click **Generate New Key**
4. Copy and securely store your API key

<Warning>
  **Security Notice:** Never expose your API key in client-side code or public repositories. Treat it like a password.
</Warning>

## Rate Limiting

API requests are rate-limited to ensure platform stability:

- **Free tier:** 100 requests/hour
- **Pro tier:** 1,000 requests/hour  
- **Enterprise tier:** 10,000 requests/hour

Rate limit information is included in response headers:

```http
X-RateLimit-Limit: 1000
X-RateLimit-Remaining: 847
X-RateLimit-Reset: 1699564800
```

## Sample API Endpoint: Create Task

### `POST /tasks`

Creates a new task in the specified project.

#### Request

**Headers**
```http
Authorization: Bearer YOUR_API_KEY
Content-Type: application/json
```

**Body Parameters**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `title` | string | Yes | Task title (max 200 characters) |
| `description` | string | No | Detailed task description |
| `project_id` | string | Yes | UUID of the parent project |
| `assignee_id` | string | No | UUID of the assigned user |
| `priority` | string | No | Task priority: `low`, `medium`, `high`, `urgent` (default: `medium`) |
| `due_date` | string | No | Due date in ISO 8601 format |
| `tags` | array | No | Array of tag strings |

**Example Request**

```bash cURL
curl -X POST https://api.taskflow.com/v1/tasks \
  -H "Authorization: Bearer tf_live_sk_abc123xyz789" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Implement user authentication",
    "description": "Add JWT-based authentication to the API",
    "project_id": "proj_9x8y7z6w5v",
    "assignee_id": "user_1a2b3c4d5e",
    "priority": "high",
    "due_date": "2025-11-15T17:00:00Z",
    "tags": ["backend", "security", "auth"]
  }'
```

```javascript JavaScript (Node.js)
const response = await fetch('https://api.taskflow.com/v1/tasks', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer tf_live_sk_abc123xyz789',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    title: 'Implement user authentication',
    description: 'Add JWT-based authentication to the API',
    project_id: 'proj_9x8y7z6w5v',
    assignee_id: 'user_1a2b3c4d5e',
    priority: 'high',
    due_date: '2025-11-15T17:00:00Z',
    tags: ['backend', 'security', 'auth']
  })
});

const data = await response.json();
console.log(data);
```

```python Python
import requests
import json

url = 'https://api.taskflow.com/v1/tasks'
headers = {
    'Authorization': 'Bearer tf_live_sk_abc123xyz789',
    'Content-Type': 'application/json'
}
payload = {
    'title': 'Implement user authentication',
    'description': 'Add JWT-based authentication to the API',
    'project_id': 'proj_9x8y7z6w5v',
    'assignee_id': 'user_1a2b3c4d5e',
    'priority': 'high',
    'due_date': '2025-11-15T17:00:00Z',
    'tags': ['backend', 'security', 'auth']
}

response = requests.post(url, headers=headers, data=json.dumps(payload))
print(response.json())
```

#### Response

**Success Response (201 Created)**

```json
{
  "id": "task_f7g8h9i0j1k2",
  "title": "Implement user authentication",
  "description": "Add JWT-based authentication to the API",
  "project_id": "proj_9x8y7z6w5v",
  "assignee_id": "user_1a2b3c4d5e",
  "priority": "high",
  "status": "todo",
  "due_date": "2025-11-15T17:00:00Z",
  "tags": ["backend", "security", "auth"],
  "created_at": "2025-11-05T08:30:00Z",
  "updated_at": "2025-11-05T08:30:00Z",
  "created_by": "user_1a2b3c4d5e",
  "url": "https://api.taskflow.com/v1/tasks/task_f7g8h9i0j1k2"
}
```

**Error Response (400 Bad Request)**

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "Invalid request parameters",
    "details": [
      {
        "field": "title",
        "message": "Title is required and cannot be empty"
      }
    ]
  }
}
```

**Error Response (401 Unauthorized)**

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or expired API key"
  }
}
```

## Response Codes

The API uses standard HTTP status codes:

| Code | Description |
|------|-------------|
| `200` | Success - Request completed successfully |
| `201` | Created - Resource successfully created |
| `400` | Bad Request - Invalid parameters or malformed request |
| `401` | Unauthorized - Invalid or missing API key |
| `403` | Forbidden - Valid credentials but insufficient permissions |
| `404` | Not Found - Resource does not exist |
| `429` | Too Many Requests - Rate limit exceeded |
| `500` | Internal Server Error - Server-side error occurred |

## Error Handling

All errors follow a consistent format:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable error message",
    "details": [] // Optional additional context
  }
}
```

### Common Error Codes

- `VALIDATION_ERROR`: Invalid input parameters
- `UNAUTHORIZED`: Authentication failed
- `FORBIDDEN`: Insufficient permissions
- `NOT_FOUND`: Resource not found
- `RATE_LIMIT_EXCEEDED`: Too many requests
- `INTERNAL_ERROR`: Server-side error

## Pagination

List endpoints support cursor-based pagination:

```bash
curl "https://api.taskflow.com/v1/tasks?limit=50&cursor=eyJpZCI6InRhc2tfMTIzIn0"
```

**Parameters:**
- `limit`: Number of items per page (default: 25, max: 100)
- `cursor`: Pagination cursor from previous response

**Response:**
```json
{
  "data": [...],
  "pagination": {
    "has_more": true,
    "next_cursor": "eyJpZCI6InRhc2tfNDU2In0"
  }
}
```

## Webhooks

TaskFlow can send real-time event notifications to your specified endpoints. Configure webhooks in your dashboard under **Settings** > **Webhooks**.

**Supported Events:**
- `task.created`
- `task.updated`
- `task.completed`
- `task.deleted`
- `project.created`
- `user.invited`

<Tip>
Webhook payloads include a `X-TaskFlow-Signature` header for verification. Always validate signatures to ensure authenticity.
</Tip>

## SDK Libraries

Official SDKs are available for popular languages:

<CardGroup cols={3}>
  <Card title="Node.js" icon="node-js">
    ```bash
    npm install @taskflow/sdk
    ```
  </Card>
  
  <Card title="Python" icon="python">
    ```bash
    pip install taskflow-sdk
    ```
  </Card>
  
  <Card title="Ruby" icon="gem">
    ```bash
    gem install taskflow
    ```
  </Card>
</CardGroup>

## Support

Need help? We're here for you:

- **Documentation:** [docs.taskflow.com](https://docs.taskflow.com)
- **API Status:** [status.taskflow.com](https://status.taskflow.com)
- **Email Support:** api-support@taskflow.com
- **Community Forum:** [community.taskflow.com](https://community.taskflow.com)

<Note>
For enterprise customers, dedicated support channels are available through your account manager.
</Note>